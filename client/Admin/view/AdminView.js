export class AdminView {
  constructor() {}
  makeAdminHeader() {
    let header = ``;
    return header;
  }

  makeProfile() {
    let profile = ``;
    return profile;
  }

  makeFooter() {
    let footer = ``;
    return footer;
  }

  makeRevisePage() {
    let revise = ``;
    return revise;
  }

  makeAddPage() {
    let add = ``;
    return add;
  }
}
