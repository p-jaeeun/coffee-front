export class CommonView {
  constructor() {}
  makeFooter() {
    let footer = ``;
    return footer;
  }

  makeProfile() {
    let profile = ``;
    return profile;
  }

  makeLoading() {
    let loading = ``;
    return loading;
  }

  makeSearPopup() {
    let pop = ``;
    return pop;
  }

  makeHalfListAndMap() {
    let half = ``;
    return half;
  }

  makeCafeinfo() {} //부분으로 나눠야 할듯
}
